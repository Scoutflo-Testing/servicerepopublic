name: Success Workflow

on:
  workflow_dispatch:

jobs:
  success-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print success message
        run: echo "This is a successful workflow!"
